# Generated by GitHub Copilot
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agent-tracer
  namespace: agent-tracer
  labels:
    app.kubernetes.io/name: agent-tracer
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - agent-tracer.example.com
        - agent-tracer-api.example.com
      secretName: agent-tracer-tls
  rules:
    - host: agent-tracer.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agent-tracer-frontend
                port:
                  number: 80
    - host: agent-tracer-api.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agent-tracer-backend
                port:
                  number: 8000
