# Generated by GitHub Copilot
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agent-tracer
  namespace: agent-tracer
spec:
  tls:
    - hosts:
        - agent-tracer.example.com
        - agent-tracer-api.example.com
      secretName: agent-tracer-prod-tls
  rules:
    - host: agent-tracer.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agent-tracer-frontend
                port:
                  number: 80
    - host: agent-tracer-api.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agent-tracer-backend
                port:
                  number: 8000
