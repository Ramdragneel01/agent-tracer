# Generated by GitHub Copilot
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agent-tracer
  namespace: agent-tracer
spec:
  tls:
    - hosts:
        - agent-tracer-stage.example.com
        - agent-tracer-api-stage.example.com
      secretName: agent-tracer-stage-tls
  rules:
    - host: agent-tracer-stage.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agent-tracer-frontend
                port:
                  number: 80
    - host: agent-tracer-api-stage.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agent-tracer-backend
                port:
                  number: 8000
